name: Build iOS IPA

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-unsigned:
    name: Unsigned IPA
    runs-on: macos-14

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.1'
          channel: 'stable'
          cache: true
          cache-key: flutter-ios-${{ runner.os }}-${{ hashFiles('ios/Podfile.lock') }}

      - name: Flutter pub get
        run: flutter pub get

      - name: Flutter analyze
        continue-on-error: true
        run: flutter analyze --no-fatal-warnings --no-fatal-infos

      - name: CocoaPods install
        run: cd ios && rm -rf Pods && pod install

      - name: Build unsigned .app
        run: flutter build ios --no-codesign --release --dart-define=SUPABASE_URL=${{ secrets.SUPABASE_URL }} --dart-define=SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}

      - name: Package unsigned IPA
        run: |
          APP="build/ios/iphoneos/Runner.app"
          echo "App bundle: $APP"
          ls -la "$APP/Info.plist" || { echo "ERROR: No Info.plist in app bundle"; exit 1; }
          rm -rf Payload
          mkdir -p Payload
          cp -R "$APP" Payload/
          ls -la Payload/Runner.app/Info.plist || { echo "ERROR: App bundle not copied correctly"; exit 1; }
          zip -r -y ai-balance-tracker-unsigned.ipa Payload
          rm -rf Payload

      - uses: actions/upload-artifact@v4
        with:
          name: ai-balance-tracker-unsigned-ipa
          path: ai-balance-tracker-unsigned.ipa
          retention-days: 30

  build-signed:
    name: Signed IPA (ad-hoc)
    runs-on: macos-14
    if: github.event_name != 'pull_request'

    steps:
      - uses: actions/checkout@v4

      - name: Check secrets
        id: check-secrets
        run: |
          if [ -z "${{ secrets.APPLE_DEVELOPER_CERTIFICATE_BASE64 }}" ]; then
            echo "has_secrets=false" >> $GITHUB_OUTPUT
            echo "::warning::Apple signing secrets not configured — skipping signed build"
          else
            echo "has_secrets=true" >> $GITHUB_OUTPUT
          fi

      - uses: subosito/flutter-action@v2
        if: steps.check-secrets.outputs.has_secrets == 'true'
        with:
          flutter-version: '3.44.1'
          channel: 'stable'
          cache: true
          cache-key: flutter-ios-${{ runner.os }}-${{ hashFiles('ios/Podfile.lock') }}

      - name: Flutter pub get
        if: steps.check-secrets.outputs.has_secrets == 'true'
        run: flutter pub get

      - name: CocoaPods install
        if: steps.check-secrets.outputs.has_secrets == 'true'
        run: cd ios && rm -rf Pods && pod install

      - name: Decode and install Apple certificate
        if: steps.check-secrets.outputs.has_secrets == 'true'
        env:
          APPLE_DEVELOPER_CERTIFICATE_BASE64: ${{ secrets.APPLE_DEVELOPER_CERTIFICATE_BASE64 }}
          APPLE_DEVELOPER_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_DEVELOPER_CERTIFICATE_PASSWORD }}
        run: |
          echo "$APPLE_DEVELOPER_CERTIFICATE_BASE64" | base64 --decode > certificate.p12
          security create-keychain -p "" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "" build.keychain
          security import certificate.p12 -k build.keychain -P "$APPLE_DEVELOPER_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple: -s -k "" build.keychain
          rm certificate.p12

      - name: Decode and install provisioning profile
        if: steps.check-secrets.outputs.has_secrets == 'true'
        env:
          APPLE_PROVISION_PROFILE_BASE64: ${{ secrets.APPLE_PROVISION_PROFILE_BASE64 }}
        run: |
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          echo "$APPLE_PROVISION_PROFILE_BASE64" | base64 --decode > ~/Library/MobileDevice/Provisioning\ Profiles/profile.mobileprovision

      - name: Build signed IPA
        if: steps.check-secrets.outputs.has_secrets == 'true'
        env:
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          cat > ios/ExportOptions.plist << 'PLIST'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>ad-hoc</string>
              <key>teamID</key>
              <string>${APPLE_TEAM_ID}</string>
              <key>compileBitcode</key>
              <false/>
              <key>uploadSymbols</key>
              <false/>
          </dict>
          </plist>
          PLIST
          flutter build ipa --export-options-plist=ios/ExportOptions.plist --release --no-tree-shake-icons --dart-define=SUPABASE_URL=${{ secrets.SUPABASE_URL }} --dart-define=SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}

      - uses: actions/upload-artifact@v4
        if: steps.check-secrets.outputs.has_secrets == 'true'
        with:
          name: ai-balance-tracker-signed-ipa
          path: build/ios/ipa/*.ipa
          retention-days: 30

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [build-unsigned]
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: ai-balance-tracker-unsigned-ipa

      - uses: softprops/action-gh-release@v2
        with:
          files: ai-balance-tracker-unsigned.ipa
          generate_release_notes: true
